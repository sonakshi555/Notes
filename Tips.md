What is the command that is used to see CPU ,memory and disk management 
- top

What is the difference between .sh and .bash and why it is?
- #!/bin/bash
- previously both were same
- if .sh was used it would redirect to .bash
- but in new version of LINUX .sh directs to .dash , as it is decided by Developers of LINUX
- so always it is preferred to use .bash , if not the script might get corrupted

What is the difference between curl and wget command?
- curl : outputs the file or contents of url
- wget : downloads and stores the contents in the system , creating a new file
